<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="stylesheet" href="/stylesheets/barras.css" />
  <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
</head>
<body>
  <% 
    const textosPreguntas = {
      pregunta1: "1. ¿Cómo califica su experiencia, en general, en el FAM TRIP a Barquisimeto?",
      pregunta2: "2. ¿Le gustaría participar nuevamente en un FAM TRIP organizado por el departamento organizador?",
      pregunta3: "3. ¿Qué aspectos considera que definieron el éxito del FAM TRIP?",
      pregunta4: "4. ¿Cómo califica la relación calidad/precio del FAM TRIP?",
      pregunta5: "5. ¿Qué lugares visitados en Barquisimeto, le pareció más interesante o relevante?",
      pregunta6: "6. ¿Considera que la duración en cada destino fue adecuada?",
      pregunta7: "7. ¿Qué lugares deben ser incluidos en el itinerario que le hubiera gustado visitar?",
      pregunta8: "8. ¿El balance entre turismo cultural, recreativo y gastronómico fue adecuado?",
      pregunta9: "9. ¿Cómo evalua la puntualidad y coordinación de los horarios del FAM TRIP?",
      pregunta10: "10. ¿El transporte utilizado durante el FAM TRIP cumplió con sus expectativas de comodidad y eficiencia?",
      pregunta11: "11. ¿Recibió toda la información necesaria sobre el itinerario, destinos y recomendaciones antes del viaje?",
      pregunta12: "12. ¿Considera que el personal de protocolo estuvo disponible y fue resolutivo?",
      pregunta13: "13. ¿Qué actividades le resultaron más significativas durante el FAM TRIP?",
      pregunta14: "14. ¿Considera que hubo un equilibrio adecuado entre actividades recreativas, culturales y gastronómicas?",
      pregunta15: "15. ¿Alguna actividad le pareció innecesaria o poco interesante?",
      pregunta16: "16. ¿Qué actividades adicionales le gustaría que se incluyan en futuros viajes?",
      pregunta17: "17. ¿Cómo califica la calidad de los hoteles y hospedajes seleccionados?",
      pregunta18: "18. ¿Considera que recibió una atención adecuada por parte del equipo organizador durante el viaje?",
      pregunta19: "19. ¿Hubo algún aspecto de los servicios ofrecidos que no cumplió con sus expectativas?",
      pregunta20: "20. ¿El FAM TRIP le ayudó a identificar oportunidades comerciales o alianzas estratégicas?",
      pregunta21: "21. ¿Qué elementos del FAM TRIP considera más relevante para su negocio o actividad profesional?",
      pregunta22: "22. ¿Recomienda este tipo de experiencia a otros colegas o socios comerciales?",
      pregunta23: "23. ¿Cree que Barquisimeto tiene potencial para posicionarse como un destino destacado en el turismo nacional e internacional?",
      pregunta24: "24. ¿Qué aspectos del destino le sorprendieron positivamente?",
      pregunta25: "25. ¿Qué aspectos del destino considera que pueden mejorarse para futuras experiencias turísticas?",
      pregunta26: "26. ¿Qué destinos o regiones recomienda visitar en próximos FAM TRIP organizados por el departamento organizador?",
      pregunta27: "27. ¿Qué valor agregado le gustaría que se incluya en futuros itinerarios?",
      pregunta28: "28. ¿Qué otras recomendaciones o sugerencias, en forma general, desea compartir con el equipo organizador?"
    };

    function calcularPorcentajes(data) {
      return data.map(mesData => {
        const total = Object.values(mesData).reduce((a,b) => a + b, 0);
        const porcentajes = {};
        for (const key in mesData) {
          porcentajes[key] = total ? (mesData[key] / total * 100).toFixed(2) : 0;
        }
        return porcentajes;
      });
    }

    // Función para ordenar las claves de preguntas numéricamente
    function ordenarPreguntas(keys) {
      return keys.sort((a, b) => {
        const numA = parseInt(a.replace('pregunta', ''), 10);
        const numB = parseInt(b.replace('pregunta', ''), 10);
        return numA - numB;
      });
    }
  %>
  <div class="container my-4">
    <header class="text-center mb-4">
      <h1>Resultados de la Encuesta</h1>
    </header>
    <hr />
    <section class="mb-3">
      <label for="mesSelector" class="form-label">Seleccione el mes:</label>
      <select id="mesSelector" class="form-select" style="max-width: 200px;">
        <% meses.forEach(function(mes) { %>
          <option value="<%= mes %>"><%= mes %></option>
        <% }); %>
      </select>
    </section>
    <section class="bars-container mb-4">
      <h3>Barras con porcentaje por pregunta y opción</h3>
      <% 
        const preguntasOrdenadas = ordenarPreguntas(Object.keys(dataPorPregunta));
        preguntasOrdenadas.forEach(function(pregunta) { 
      %>
        <h4><%= textosPreguntas[pregunta] || pregunta %></h4>
        <% 
          const opciones = opcionesRespuestaPorPregunta[pregunta];
          const promedioPorOpcion = {};
          opciones.forEach(opcion => {
            let suma = 0;
            let count = 0;
            meses.forEach(mes => {
              if (dataPorPregunta[pregunta][mes]) {
                suma += parseFloat(dataPorPregunta[pregunta][mes][opcion]) || 0;
                count++;
              }
            });
            promedioPorOpcion[opcion] = count ? (suma / count).toFixed(2) : 0;
          });
        %>
        <% opciones.forEach(function(opcion) { %>
          <div class="bar-wrapper" data-pregunta="<%= pregunta %>" data-opcion="<%= opcion %>">
            <div class="bar-label"><%= opcion %></div>
            <div class="bar">
              <div class="bar-fill" data-width="<%= promedioPorOpcion[opcion] + '%' %>">
                <%= promedioPorOpcion[opcion] %>%
              </div>
            </div>
          </div>
        <% }); %>
      <% }); %>
    </section>
    <hr />
    <section class="mb-3">
      <button class="btn btn-primary" onclick="window.print()">Imprimir / Guardar como PDF</button>
    </section>
    <section class="mb-3">
      <a href="/" class="btn btn-secondary">Volver a la Encuesta</a>
    </section>
  </div>

  <script>
    function decodeBase64Json(base64) {
      const jsonString = atob(base64);
      return JSON.parse(jsonString);
    }

    const mesesBase64 = '<%= Buffer.from(JSON.stringify(meses)).toString("base64") %>';
    const dataPorPreguntaBase64 = '<%= Buffer.from(JSON.stringify(dataPorPregunta)).toString("base64") %>';
    const opcionesRespuestaPorPreguntaBase64 = '<%= Buffer.from(JSON.stringify(opcionesRespuestaPorPregunta)).toString("base64") %>';

    const meses = decodeBase64Json(mesesBase64);
    const dataPorPregunta = decodeBase64Json(dataPorPreguntaBase64);
    const opcionesRespuestaPorPregunta = decodeBase64Json(opcionesRespuestaPorPreguntaBase64);

    document.addEventListener('DOMContentLoaded', () => {
      const barras = document.querySelectorAll('.bar-fill[data-width]');
      barras.forEach(barra => {
        const width = barra.getAttribute('data-width');
        barra.style.width = width;
        barra.style.setProperty('--width', width);
      });
    });
  </script>

  <script>
    function aplicarAnchoBarras() {
      const barras = document.querySelectorAll('.bar-fill[data-width]');
      barras.forEach(barra => {
        const width = barra.getAttribute('data-width');
        barra.style.width = width;
        barra.style.setProperty('--width', width);
      });
    }
    document.addEventListener('DOMContentLoaded', aplicarAnchoBarras);
    window.addEventListener('beforeprint', aplicarAnchoBarras);
  </script>

  <style>
    #comentarios-section {
      margin: 2rem auto;
      max-width: 900px;
      background: rgba(255, 255, 255, 0.9);
      padding: 1rem;
      border-radius: 8px;
      display: none;
    }
    #comentarios-toggle {
      display: block;
      margin: 1rem auto;
      padding: 0.5rem 1rem;
      background-color: var(--bs-primary);
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
    }
    .comentario-encuestado {
      border-bottom: 1px solid #dee2e6;
      padding: 0.5rem 0;
    }
    .comentario-encuestado:last-child {
      border-bottom: none;
    }
    .comentario-encuestado h5 {
      margin: 0 0 0.5rem 0;
      font-weight: 600;
      color: #6c757d;
    }
    .comentario-encuestado p {
      margin: 0.25rem 0;
      white-space: pre-wrap;
    }
  </style>

  <button id="comentarios-toggle" class="btn btn-primary">Mostrar/Ocultar Sugerencias Opcionales</button>
  <section id="comentarios-section" class="container">
    <h3>Sugerencias y Comentarios Opcionales por Encuestado</h3>
    <% if (comentarios && comentarios.length > 0) { %>
      <% comentarios.forEach(function(c) { %>
        <div class="comentario-encuestado" data-fecha="<%= new Date(c.fechaRegistro).toISOString() %>">
          <h5><%= c.nombreCompleto || 'Encuestado Anónimo' %> - <small><%= new Date(c.fechaRegistro).toLocaleDateString() %></small></h5>
          <% if (c.pregunta5_otro) { %><p><strong>Pregunta 5 - Otro lugar:</strong> <%= c.pregunta5_otro %></p><% } %>
          <% if (c.pregunta4_comentarios) { %><p><strong>Pregunta 4:</strong> <%= c.pregunta4_comentarios %></p><% } %>
          <% if (c.pregunta10_comentarios) { %><p><strong>Pregunta 10:</strong> <%= c.pregunta10_comentarios %></p><% } %>
          <% if (c.pregunta12_comentarios) { %><p><strong>Pregunta 12:</strong> <%= c.pregunta12_comentarios %></p><% } %>
          <% if (c.pregunta14_comentarios) { %><p><strong>Pregunta 14:</strong> <%= c.pregunta14_comentarios %></p><% } %>
          <% if (c.pregunta18_comentarios) { %><p><strong>Pregunta 18:</strong> <%= c.pregunta18_comentarios %></p><% } %>
          <% if (c.pregunta19_comentarios) { %><p><strong>Pregunta 19:</strong> <%= c.pregunta19_comentarios %></p><% } %>
          <% if (c.pregunta26_comentarios) { %><p><strong>Pregunta 26:</strong> <%= c.pregunta26_comentarios %></p><% } %>
          <% if (c.pregunta27_comentarios) { %><p><strong>Pregunta 27:</strong> <%= c.pregunta27_comentarios %></p><% } %>
          <% if (c.pregunta28_comentarios) { %><p><strong>Pregunta 28:</strong> <%= c.pregunta28_comentarios %></p><% } %>
        </div>
      <% }); %>
    <% } else { %>
      <p>No hay sugerencias o comentarios disponibles.</p>
    <% } %>
  </section>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>

  <script>
    document.getElementById('comentarios-toggle').addEventListener('click', function() {
      const section = document.getElementById('comentarios-section');
      if (section.style.display === 'none' || section.style.display === '') {
        section.style.display = 'block';
        this.textContent = 'Ocultar Sugerencias Opcionales';
      } else {
        section.style.display = 'none';
        this.textContent = 'Mostrar Sugerencias Opcionales';
      }
    });

    // Filtrar resultados por mes
    document.getElementById('mesSelector').addEventListener('change', function() {
      const mesSeleccionado = this.value;

      // Filtrar barras
      const barras = document.querySelectorAll('.bar-wrapper');
      barras.forEach(barra => {
        const pregunta = barra.getAttribute('data-pregunta');
        const opcion = barra.getAttribute('data-opcion');
        const dataMes = dataPorPregunta[pregunta] && dataPorPregunta[pregunta][mesSeleccionado];
        if (dataMes && dataMes[opcion] !== undefined) {
          const total = Object.values(dataMes).reduce((a,b) => a + b, 0);
          const porcentaje = total ? (dataMes[opcion] / total * 100).toFixed(2) : 0;
          barra.querySelector('.bar-fill').style.width = porcentaje + '%';
          barra.querySelector('.bar-fill').textContent = porcentaje + '%';
          barra.style.display = 'block';
        } else {
          barra.style.display = 'none';
        }
      });

      // Filtrar comentarios
      const comentarios = document.querySelectorAll('.comentario-encuestado');
      comentarios.forEach(comentario => {
        const fechaRegistroISO = comentario.getAttribute('data-fecha');
        const mesComentario = fechaRegistroISO ? fechaRegistroISO.slice(0,7) : '';
        if (mesComentario === mesSeleccionado) {
          comentario.style.display = 'block';
        } else {
          comentario.style.display = 'none';
        }
      });
    });

    // Seleccionar el mes más reciente por defecto
    document.addEventListener('DOMContentLoaded', () => {
      const mesSelector = document.getElementById('mesSelector');
      if (mesSelector.options.length > 0) {
        mesSelector.value = mesSelector.options[mesSelector.options.length - 1].value;
        mesSelector.dispatchEvent(new Event('change'));
      }
    });
  </script>
</body>
</html>